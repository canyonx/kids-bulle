{% extends 'base.html.twig' %}

{% block title %}{{"New_user"|trans}}{% endblock %}

{% block body %}
    <div class="card col-6 mx-auto">
        <div class="card-header">
            <h1 class="card-title">{{"New_user"|trans}}</h1>
            <a class="btn btn-light" href="{{ path('app_admin_user_index') }}">{{"back_to_list"|trans}}</a>
        </div>
        <div class="card-body">
            {{ include('admin/user/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
