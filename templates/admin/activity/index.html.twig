{% extends 'base.html.twig' %}

{% block title %}Activity index{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">Activity index</h1>
            <a class="btn btn-light" href="{{ path('app_admin_activity_new') }}">Create new</a>
        </div>
        <div class="card-body">
            {% include "shared/_planning.html.twig" %}
        </div>
    </div>
{% endblock %}
