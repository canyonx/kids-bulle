{% extends 'base.html.twig' %}

{% block title %}Edit Activity{% endblock %}

{% block body %}
    <div class="card col-6 mx-auto">
        <div class="card-header">
            <h1 class="card-title">Edit Activity</h1>
            <a class="btn btn-light" href="{{ path('app_admin_activity_index') }}">back to list</a>
        </div>
        <div class="card-body">
            {{ include('admin/activity/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="card-footer text-end">
            {{ include('admin/activity/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
