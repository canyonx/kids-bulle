<nav class="navbar navbar-expand mb-4 bg-light" aria-label="Second navbar example">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('app_home') }}">Kid's Bulle</a>

        <div class="collapse navbar-collapse" id="navbarsExample02">
           
            {% if is_granted('ROLE_USER') %}
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user') }}">{{"Profile"|trans}}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user_activities')}}">{{"Planning"|trans}}</a>
                    </li>
                </ul>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin_activity_index') }}">{{"Activities"|trans}}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin_category_index') }}">{{"Categories"|trans}}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin_user_index') }}">{{"Users"|trans}}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin_child_index') }}">{{"Childs"|trans}}</a>
                    </li>
                </ul>
            {% endif %}

            <ul class="navbar-nav ms-auto">
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">{{"Log_out"|trans}}</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_register') }}">{{"Sign_in"|trans}}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_login') }}">{{"Log_in"|trans}}</a>
                    </li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>