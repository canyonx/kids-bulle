{% extends 'base.html.twig' %}

{% block title %}{{"New_Child"|trans}}{% endblock %}

{% block body %}
    <div class="card col-md-6 mx-auto">
        <div class="card-header">
            <h1 class="card-title">{{"New_Child"|trans}}</h1>
            {{ component('BackButton', { url: path('app_user') }) }}
        </div>
        <div class="card-body">
            {{ include('children/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
