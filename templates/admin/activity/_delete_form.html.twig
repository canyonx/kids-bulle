<form method="post" action="{{ path('app_admin_activity_delete', {'id': activity.id}) }}" onsubmit="return confirm('Supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity.id) }}">
    <button class="btn btn-danger" alt="{{"Delete"|trans}}"><i class="fa-solid fa-trash-can"></i></button>
</form>
