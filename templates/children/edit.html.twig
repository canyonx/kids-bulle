{% extends 'base.html.twig' %}

{% block title %}Edit Child{% endblock %}

{% block body %}
    <h1>Edit Child</h1>

    {{ include('children/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user') }}">back to profile</a>

    {{ include('children/_delete_form.html.twig') }}
{% endblock %}
