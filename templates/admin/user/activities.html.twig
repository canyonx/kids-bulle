{% extends 'base.html.twig' %}

{% block title %}Famille de {{ user }}{% endblock %}

{% block body %}
        <div class="card">
            <div class="card-header">
                <h1 class="card-title">Famille de {{ user }}</h1>
            </div>
            <div class="card-body">
                {% if user.childs|length > 0 or is_granted("ROLE_TEACHER") %}
                    {# <turbo-frame id="planning_family"> #}
                        {% include "planning/_nav_month.html.twig" %}
                        {% include "planning/_planningFamily.html.twig" %}
                    {# </turbo-frame> #}
                {% else %}
                    <p>{{"No_activities_in_your_planning"|trans}} !</p>
                {% endif %}
            </div>
        </div>
{% endblock %}