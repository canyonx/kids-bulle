{% if activity.dateAt|date("i") is not same as "00" %}
    {{ activity.dateAt|date("H\\hi") }}
{% else %}
    {{ activity.dateAt|date("H\\h") }}
{% endif %}

{% if activity.title %} 
    {{ activity.title }} 
{% else %}
    {{ activity.category }}  
{% endif %}