{% extends 'base.html.twig' %}

{% block title %}Configuration{% endblock %}

{% block body %}
    <div class="card col-md-6 mx-auto">
        <div class="card-header">
            <h1 class="card-title">
                Configuration
            </h1>
        </div>
        <div class="card-body">
            {{ include('admin/config/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
{% endblock %}
