{% extends 'base.html.twig' %}

{% block title %}{{"New_Child"|trans}}{% endblock %}

{% block body %}
    <div class="card col-6 mx-auto">
        <div class="card-header">
            <h1 class="card-title">{{"New_Child"|trans}}</h1>
            <a class="btn btn-light" href="{{ path('app_user') }}">{{"back_to_profile"|trans}}</a>
        </div>
        <div class="card-body">
            {{ include('children/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
