<form method="post" action="{{ path('app_children_delete', {'id': child.id}) }}" onsubmit="return confirm('Supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child.id) }}">
    <button class="btn btn-danger">{{"Delete"|trans}}</button>
</form>
