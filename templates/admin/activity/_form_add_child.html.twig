{{ form_start(form) }}
<div class="d-flex align-items-start">
    {{ form_widget(form, {attr: {class: 'w-100'}}) }}
    <button class="btn btn-primary ms-2" alt="Ajouter"><i class="fa-solid fa-plus"></i></button>
</div>
{{ form_end(form) }}
