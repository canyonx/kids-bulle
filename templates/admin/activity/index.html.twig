{% extends 'base.html.twig' %}

{% block title %}{{"Activity_index"|trans}}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">{{"Activity_index"|trans}}</h1>
            <a class="btn btn-light" href="{{ path('app_admin_activity_new') }}">{{"Create_new"|trans}}</a>
        </div>
        <div class="card-body">
            {% include "shared/_planning.html.twig" %}
        </div>
    </div>
{% endblock %}
