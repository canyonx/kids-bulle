{% extends 'base.html.twig' %}

{% block title %}{{"Planning_of"|trans}} {{ child.firstname }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">{{"Planning_of"|trans}} {{ child.firstname }}</h1>
            <a class="btn btn-light" href="{{ path('app_user_activities') }}">{{"back_to_planning"|trans}}</a>
        </div>
        <div class="card-body">
            {% include "shared/_planningChild.html.twig" %}
        </div>
    </div>
{% endblock %}
