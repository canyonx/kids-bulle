<nav class="navbar navbar-expand" aria-label="Second navbar example" style="background-color: #e3f2fd;">
    <div class="container-fluid">

        <a class="navbar-brand" href="#">Kid's Bulle</a>

        <div class="collapse navbar-collapse" id="navbarsExample02">
           
            {% if is_granted('ROLE_USER') %}
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_user') }}">Mon profil</a>
                    </li>
                </ul>
            {% endif %}

            <ul class="navbar-nav ms-auto">
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_logout') }}">Se deconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="#">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>