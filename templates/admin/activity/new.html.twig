{% extends 'base.html.twig' %}

{% block title %}{{"New_activity"|trans}}{% endblock %}

{% block body %}
    <div class="card col-md-6 mx-auto">
        <div class="card-header">
            <h1 class="card-title">{{"New_activity"|trans}}</h1>
            <a class="btn btn-link" href="{{ path('app_planning') }}">{{"back_to_list"|trans}}</a>
        </div>
        <div class="card-body">
            {{ include('admin/activity/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
